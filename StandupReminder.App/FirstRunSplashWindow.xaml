<Window x:Class="StandupReminder.App.FirstRunSplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Standup Reminder"
        Width="400"
        Height="360"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="20"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0" />
    </shell:WindowChrome.WindowChrome>
    <Border Background="#FF111315"
            BorderBrush="#22282D"
            BorderThickness="1"
            CornerRadius="20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>

            <Border CornerRadius="20,0,0,20" ClipToBounds="True">
                <Image x:Name="SplashImage"
                       Source="pack://application:,,,/Assets/splash.png"
                       Stretch="Fill" />
            </Border>

            <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock FontSize="14"
                           Foreground="#FF7BA7FF"
                           Text="StandupReminder" />

                <TextBlock Grid.Row="2"
                           Margin="0,16,0,0"
                           TextWrapping="Wrap"
                           FontSize="15"
                           Foreground="#FFD0D7DE"
                           Text="Keeps running in the system tray" />                
            </Grid>
        </Grid>
    </Border>
</Window>

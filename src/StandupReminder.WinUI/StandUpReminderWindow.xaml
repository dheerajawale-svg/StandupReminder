<Window
    x:Class="StandupReminder.WinUI.StandUpReminderWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Time to stand up">
    <Grid Background="Transparent">
        <Border
            x:Name="ReminderBackgroundBorder"
            Margin="12"
            Padding="24"
            CornerRadius="16"
            Background="#B81E1E1E">
            <Grid RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock FontSize="28" FontWeight="SemiBold" Foreground="White" Text="Time to stand up" />
                <TextBlock Grid.Row="1" FontSize="16" Foreground="#E6FFFFFF" HorizontalAlignment="Center" Text="Take a break from sitting." />
                <Border Grid.Row="2" Padding="16" Background="#14FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="12">
                    <TextBlock x:Name="StandDurationTextBlock" FontSize="15" Foreground="White" TextWrapping="WrapWholeWords" />
                </Border>
                <Grid Grid.Row="3" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1" Width="120" Click="OnSnoozeClicked" Content="Snooze 5 min" />
                    <Button Grid.Column="2" Width="160" Click="OnConfirmClicked" Content="I stood up" Style="{StaticResource AccentButtonStyle}" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>